# Comma-separated list of tickers to track
TICKERS=AAPL,MSFT,TSLA

# Postgres connection string (e.g., for Vercel Postgres or Supabase)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: allow hitting /api/cron outside production for manual testing
ALLOW_CRON=true
